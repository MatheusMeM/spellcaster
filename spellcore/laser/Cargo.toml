[package]
name = "laser"
version.workspace = true
edition.workspace = true

[dependencies]
# beacon Ether Dream (UDP 7654) ja existe em protocols::netscan; reusar em vez de reimplementar
protocols = { workspace = true }
# Safety/Transform sao lidos do bloco "safety"/"keys" do track de laser no .spell
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
criterion = { workspace = true }

[[bin]]
name = "feeds"
path = "src/bin/feeds.rs"

[[bench]]
name = "laser"
harness = false
